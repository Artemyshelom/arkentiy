# Отчёт: Очистка корневой папки — 12 марта 2026

> Результаты рефакторинга структуры проекта: релокация файлов и документации.

---

## Что было сделано

### ✅ Завершено

| # | Действие | Статус | Детали |
|----|---------|--------|--------|
| 1 | Удалена папка `.tz/` | ✅ | Содержала один черновой файл `jobs_health_dashboard.md` |
| 2 | Файл `jobs_health_dashboard.md` перемещён | ✅ | Из `.tz/` → `docs/specs/tg/jobs_health_dashboard.md` |
| 3 | Обновлена документация `docs/README.md` | ✅ | Добавлено уточнение про PROJECT_UPDATE_STATUS |
| 4 | Обновлена маска `.gitignore` | ✅ | Добавлены: `backups/*/`, `.tz/`, `*.tmp` |
| 5 | Проверена локация `CHANGELOG.md` | ✅ | Уже находится в `docs/CHANGELOG.md` |

---

## Состояние корневой папки (после)

### ✅ Остаётся в корне (обязательно)

```
arkentiy/
├── docker-compose.yml       # Orchest контейнеров
├── Dockerfile              # Сборка образа Python
├── requirements.txt        # Зависимости
├── .env.example            # Шаблон конфига
├── README.md              # Основная документация
├── PROJECT_UPDATE_STATUS.md # ✅ Снапшот статуса (дополняет journal.md)
└── .gitignore             # Маски исключения
```

### 📁 Остаётся в корне (подпапки)

```
├── app/                # Python приложение (основной код)
├── web/                # Фронтенд (HTML + JS)
├── docs/               # Полная документация (журнал, specs, onnboarding)
├── rules/              # Правила разработки
├── assets/             # Статика (аватары и т.п.)
├── nginx/              # Конфиг nginx
├── .git/               # Git репозиторий
├── .github/            # GitHub Actions
├── .vscode/            # VS Code settings
└── secrets/            # Переменные окружения (в .gitignore)
```

### ❌ Удалено

```
.tz/                    # Черновики (удалена)
```

### 🔄 Перемещено

```
jobs_health_dashboard.md
  ✅ Было: .tz/jobs_health_dashboard.md
  ✅ Стало: docs/specs/tg/jobs_health_dashboard.md
  📝 Назначение: ТЗ на реализацию фичи (уже реализовано в сессии 06.03)
```

---

## Обновлённая документация

### `docs/README.md`
- ✅ Обновлена навигация
- ✅ Добавлено уточнение: `PROJECT_UPDATE_STATUS.md` — дополнительный снапшот на корневом уровне

### `.gitignore`
- ✅ Добавлены маски:
  - `backups/*/` — локальные снимки БД
  - `.tz/` — черновики и временные файлы
  - `*.tmp` — временные файлы

### `docs/specs/tg/jobs_health_dashboard.md`
- ✅ Создан файл
- ✅ Содержит полное ТЗ на функцию
- ✅ Отмечено: реализовано в сессии 06.03.2026

---

## Результаты

### 📊 Метрики

| Параметр | До | После | Изменение |
|----------|----|----|-----------|
| Файлов в корне | 7 | 7 | ±0 |
| Папок в корне | 11 | 10 | -1 |
| Чистоты корня | 64% | 70% | +6% |
| Файлов в docs/specs | 3 | 4 | +1 |

### ✨ Преимущества

1. **Порядок** — .tz папка (черновики) теперь удалена
2. **Навигация** — jobs_health_dashboard в правильной категории (specs/tg)
3. **Безопасность** — backups/ исключены из git (потенциал утечки данных)
4. **Ясность** — dokumentация чётко объясняет роль PROJECT_UPDATE_STATUS

---

## Проверка git статуса

Для применения обновлений в git:

```bash
# Убедитесь что нет незафиксированных изменений
git status

# Фиксация изменений
git add .
git commit -m "cleanup: удалена .tz папка, перемещена jobs_health_dashboard в docs/specs/tg"
```

---

## Следующие шаги (если требуются)

1. **Удаление backups/** с VPS (если там есть) — не хранить снимки БД в production
2. **Проверить** есть ли на VPS артефакты в корневой папке `/opt/ebidoebi/`
3. **Документировать** структуру VPS в `rules/integrator/infrastructure.md`

---

*Дата: 12 марта 2026*  
*Автор: Интегратор*
