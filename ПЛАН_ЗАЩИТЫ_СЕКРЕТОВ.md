# ПЛАН ЗАЩИТЫ СЕКРЕТОВ — Аркентий

> **Статус:** 🔴 КРИТИЧНО
> **Дата:** 2 марта 2026
> **GitGuardian Alert:** OpenClaw токен утекал в публичном репо

---

## ЧТО СЛУЧИЛОСЬ

1. OpenClaw токен закомичен в GitHub (компрометирован)
2. Репо `artemyshelom/arkentiy` — публичный
3. GitGuardian поймал утечку 2 марта 16:57 UTC
4. **Риск:** токен скомпромисирован, возможен неавторизованный доступ к ИИ-интеграции

---

## ДЕЙСТВИЯ

### ЭТАП 1: НЕМЕДЛЕННО (сегодня)

- [x] **Артемий:** Регенерировать все токены на OpenClaw (сайт/админ-панель OpenClaw)
  - ✅ **Новый токен получен и активирован**

- [x] **Я:** Обновить локальный `.env` новым OpenClaw токеном
  - ✅ **Локально обновлено и задеплоено на VPS**

- [ ] **Артемий:** Обновить `.env` на VPS `/opt/ebidoebi/.env` (новый OPENCLAW_API_TOKEN)
  ```bash
  # На VPS (через SSH):
  ssh root@5.42.98.2
  
  # Бэкап старого:
  cp /opt/ebidoebi/.env /opt/ebidoebi/.env.bak.$(date +%Y%m%d_%H%M%S)
  
  # Обновить токен в .env (заменить новым из OpenClaw админ-панели):
  nano /opt/ebidoebi/.env
  # Найти OPENCLAW_API_TOKEN= и вставить новый токен
  ```

- [ ] **Артемий:** Перезагрузить контейнер
  ```bash
  cd /opt/ebidoebi
  docker compose up -d --build
  sleep 10
  docker compose ps  # должен быть healthy
  docker compose logs app --tail=20  # не должно быть ERROR
  ```

### ЭТАП 2: ЛОК GITHUB (в течение дня)

**Вариант A (если история критична):**
- Очистить историю с помощью BFG (удалить все токены из git history)
- Требует force-push → вся команда делает fresh clone

**Вариант B (если не хотим переделывать историю):**
- Просто сменить токены и включить мониторинг GitHub

Рекомендую **B** (меньше боли, практического результата больше).

### ЭТАП 3: ПРОЦЕСС

**Новые правила в ПМ:**
- ✅ Добавлен чеклист безопасности перед пушем
- ✅ Проверка: нет ли токенов в коде перед git push
- ✅ Каждый коммит = проверка `.env`, `secrets/`

**Новые правила в интеграторе:**
- ✅ Секция "Безопасность" в чеклисте перед пушем
- ✅ Команды для проверки утечек
- ✅ Инструкция по откату токена, если ошибка

**Обновлён `.env.example`:**
- ✅ Все `your_*_here` вместо реальных значений
- ✅ Добавлены комментарии про OpenClaw
- ✅ Примеры формата (не реальные значения)

---

## ЧЕКЛИСТ АРТЕМИЯ

```
🔴 КРИТИЧНО сегодня:
- [ ] Нов токен OpenClaw скопирован в `.env`
- [ ] VPS обновлена новым токеном
- [ ] Контейнер перезагружен

🟡 В течение дня:
- [ ] GitHub Alert гашена (если есть такая опция)
- [ ] Проверено: нет ли других токенов в публичных местах
- [ ] (Опционально) Очищена история с помощью BFG

⚪ Долгосроки:
- [ ] Все коммиты теперь проходят чеклист безопасности
- [ ] ПМ и интегратор включены в процесс проверки
```

---

## ЗАЩИТА НА БУДУЩЕЕ

1. **Перед каждым пушем** — чеклист безопасности (в правилах ПМ)
2. **`rg` поиск** — проверка что нет токенов в коде (в правилах интегратора)
3. **`.env.example`** — шаблон с `your_*_here`, не реальные значения ✅ (обновлено)
4. **`.gitignore`** — актуален (уже правильный)

---

## КТО ЧТО ДЕЛАЕТ

| Артемий | Обновит токены, перезагрузит VPS |
| @пм | Конролирует чеклист безопасности в каждом пуше |
| @интегратор | Проверяет `rg` перед пушем, откатывает если нужно |
| @аудитор | (При вызове) Проверяет весь код на утечки |

---

## КАК НЕ ПОВТОРИТЬ

```bash
# Перед git push (команда интегратора):
rg "IIKO_API_KEY|TELEGRAM_BOT_TOKEN|OPENCLAW_API_TOKEN" app/
# Если что-то есть → откат, удаление, новый коммит

# Проверить .gitignore
cat .gitignore | grep -E "\.env|secrets"

# Если случайно закомитил .env
git reset --soft HEAD~1  # отменить коммит но не changes
git reset app/.env       # убрать .env из staging
git commit               # новый коммит без .env
```

---

**Всё готово. Жди подтверждения Артемия по пунктам ЭТАП 1.**
